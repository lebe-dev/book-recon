services:
  book-recon:
    image: tinyops/book-recon:0.1.0
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /app/data/book-recon.db
    volumes:
      - ./data:/app/data
